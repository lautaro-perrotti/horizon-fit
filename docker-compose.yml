services:
  db:
    image: mariadb:10.6
    container_name: horizon-fit-db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: horizon_fit
      MYSQL_USER: horizon_fit
      MYSQL_PASSWORD: horizon_fit
      MYSQL_ROOT_PASSWORD: root
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
    ports:
      - "3308:3306"
    volumes:
      - horizon_fit_db:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 10

  wordpress:
    image: wordpress:6.9.4-php8.2-apache
    container_name: horizon-fit-wp
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8088:80"
    environment:
      WORDPRESS_DB_HOST: db:3306
      WORDPRESS_DB_NAME: horizon_fit
      WORDPRESS_DB_USER: horizon_fit
      WORDPRESS_DB_PASSWORD: horizon_fit
      WORDPRESS_TABLE_PREFIX: hf_
      HF_STOREFRONT_SOURCE_PATH: /var/www/html/index.html
      WORDPRESS_CONFIG_EXTRA: |
        define('WP_HOME', 'http://localhost:8088');
        define('WP_SITEURL', 'http://localhost:8088');
        define('WP_REST_ALLOWED_HOSTS', ['localhost:8088', '*']);
        define('FS_METHOD', 'direct');
        define('WP_MEMORY_LIMIT', '256M');
    volumes:
      - horizon_fit_html:/var/www/html
      - ./index.html:/var/www/html/index.html:ro
      - ./design-system:/var/www/html/design-system:ro
      - ./assets:/var/www/html/assets:ro
      - ./TIPOGRAFIAS:/var/www/html/TIPOGRAFIAS:ro
      - ./backend/wordpress/wp-content/plugins:/var/www/html/wp-content/plugins:ro
      - ./backend/wordpress/wp-content/uploads:/var/www/html/wp-content/uploads
      - ./backend/wordpress/wp-content/themes/horizon-fit-blank:/var/www/html/wp-content/themes/horizon-fit-blank:ro
      - ./.htaccess:/var/www/html/.htaccess:ro
      - ./docker/apache/horizon-fit.conf:/etc/apache2/sites-available/000-default.conf:ro
      - ./docker/php/uploads.ini:/usr/local/etc/php/conf.d/uploads.ini:ro

  wpcli:
    image: wordpress:cli-php8.2
    container_name: horizon-fit-wpcli
    depends_on:
      db:
        condition: service_healthy
      wordpress:
        condition: service_started
    working_dir: /var/www/html
    entrypoint: ["wp"]
    environment:
      WORDPRESS_DB_HOST: db:3306
      WORDPRESS_DB_NAME: horizon_fit
      WORDPRESS_DB_USER: horizon_fit
      WORDPRESS_DB_PASSWORD: horizon_fit
      WORDPRESS_TABLE_PREFIX: hf_
      HF_STOREFRONT_SOURCE_PATH: /var/www/html/index.html
    volumes:
      - ./backend/wordpress:/var/www/html
      - ./index.html:/var/www/html/index.html:ro
      - ./design-system:/var/www/html/design-system:ro
      - ./assets:/var/www/html/assets:ro
      - ./TIPOGRAFIAS:/var/www/html/TIPOGRAFIAS:ro

  phpmyadmin:
    image: phpmyadmin:5-apache
    container_name: horizon-fit-pma
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8089:80"
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      PMA_USER: root
      PMA_PASSWORD: root

volumes:
  horizon_fit_db:
  horizon_fit_html:
